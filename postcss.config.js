module.exports = {
    plugins: [require("tailwindcss"), require("autoprefixer")],
  }


  //64 in prod -> in yellow -> are the ones that are from the first bunch but new they are not configured -> all the are new -> front 3 are on what we will work on -> 6.10 prod me -> make it generic counts can increase -> 